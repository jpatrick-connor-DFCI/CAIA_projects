# CAIA / COMPASS

Research workflow for assembling a prostate-cancer cohort from local DFCI / Profile / OncDRS
exports, phenotyping each patient with a single-call LLM classifier (`NEPC_classifier`), and
running landmark survival analysis (Cox / XGBoost / Dynamic-DeepHit) on the resulting cohort.

The code is pandas-based. Entry points are command-line scripts orchestrated from per-arm
`run_locally.ipynb` notebooks; figures are produced by `generate_figures.ipynb`.

> This README is the canonical reference for editing the pipeline. It documents the directory
> layout, the data flow, every script's inputs/outputs, the **conventions and invariants that
> must be preserved** (split discipline, train-only fitting, landmark base, ID-column handling),
> and a list of **known issues / footguns**. Read the "Conventions & invariants" and "Known
> issues" sections before changing modeling or data-prep code.

---

## Repository structure

Only `COMPASS/` (and a stub `IPIO/`) is tracked — see `.gitignore` (`/*` is ignored except an
allowlist). `common_OMOP/` exists on disk but is **not** tracked.

```text
COMPASS/
├── utils.py                              # note-cleaning regex rules + clean_note()
├── OMOP_to_DFCI_lab_ids.csv              # lab-id → OMOP mapping (consumed by consolidate_dfci_labs)
├── unique_lab_ids_w_units.csv            # lab name/unit inventory (regenerated by longitudinal step)
│
├── data_preprocessing/                   # raw export → prostate cohort source CSVs
│   ├── compile_prostate_data.py          # ENTRY: build prostate_* source tables
│   ├── compile_prostate_notes.py         # ENTRY: prostate MRNs → prostate_text_data.csv (raw OncDRS)
│   ├── compile_MRNs_for_manual_review.py # build review sheet (platinum + ICD + PARPi + BRCA2)
│   └── compile_text_for_LLM_review.py    # select ±90d notes for LLM review
│
├── NEPC_classifier/                      # single-call LLM phenotyping (see its own README)
│   ├── README.md                         # ← authoritative docs for this stage
│   ├── helpers.py                        # config, env vars, triggers, prompt, Azure client, note loaders
│   ├── compile_prostate_note_bundle.py   # (optional) pre-compile notes → gzip bundle
│   └── run_NEPC_classifier.py            # ENTRY: classify patients → labels.tsv
│
├── longitudinal_LLM/                     # per-note LLM extraction → patient timelines
│   ├── helpers.py                        # per-note snippet iteration; reuses NEPC_classifier plumbing
│   ├── extract_gleason_timeline.py       # ENTRY: every Gleason score + date per patient
│   └── extract_avpc_nepc_timeline.py     # ENTRY: AVPC C1–C7 / NEPC criteria onset timeline
│
└── survival_analysis/
    ├── helpers/                          # SHARED library used by both arms
    │   ├── helper.py                     # canonical-lab selection, horizon grid, Brier, CV, leakage guards
    │   ├── loaders.py                    # load_profile_longitudinal / load_caia_parquet
    │   ├── plotting.py                   # lab taxonomy, IRNT, overlay hist/KM, Wilson CI
    │   └── consolidate_dfci_labs.py      # unit standardization + sentinel/physiologic filtering
    │
    ├── PROFILE/                          # the primary analysis arm (real implementations)
    │   ├── longitudinal_data_processing.py  # ENTRY 1: raw exports → longitudinal_prediction_data.csv
    │   ├── build_prediction_inputs.py       # ENTRY 2: landmark cohorts, split, canonical labs, horizons
    │   ├── build_genomic_inputs.py          # ENTRY 2b: t_sample-anchored genomic arm inputs
    │   ├── cox_aggregated.py                # ENTRY 3: Cox (univariate + elastic-net multivariable) + shared lib
    │   ├── landmark_xgboost.py              # ENTRY 3: XGBoost survival:cox
    │   ├── dynamic_deephit.py               # ENTRY 3: GRU competing-risks DeepHit
    │   ├── cox_pgs_adjusted.py              # ENTRY 3b: PGS-adjusted Cox sensitivity sweep
    │   ├── cox_genomic_univariate.py        # ENTRY 3b: univariate Cox on genomic arm
    │   ├── run_locally.ipynb                # driver: runs stages 1→3 end-to-end
    │   └── generate_figures.ipynb           # 4 platinum-arm figures
    │
    └── CAIA/                             # thin wrappers over PROFILE (parquet cohort)
        ├── load_caia_parquet.py             # re-export shim → helpers.loaders.load_caia_parquet
        ├── build_prediction_inputs.py       # injects CAIA defaults, runpy → PROFILE module
        ├── cox_aggregated.py                # "
        ├── landmark_xgboost.py              # "
        ├── dynamic_deephit.py               # "
        ├── summary_figures.py               # standalone PROFILE-vs-CAIA cohort comparison figures
        ├── run_locally.ipynb / generate_figures.ipynb / summary_figures.ipynb
        └── __init__.py
```

---

## Data flow

```text
 raw DFCI / OncDRS / Profile exports  (/data/gusev/USERS/jpconnor/data/...)
        │
        ▼  data_preprocessing/compile_prostate_data.py
 prostate_text_data.csv, prostate_icd_data.csv, prostate_health_history_data.csv,
 prostate_medications_data.csv, prostate_labs_data.csv, prostate_somatic_data.csv,
 total_psa_records.csv, platinum_chemo_records.csv
        │                                        │
        │ (notes)                                │ (labs / icd / meds / death / platinum)
        ▼                                        ▼
 NEPC_classifier/run_NEPC_classifier.py    survival_analysis/PROFILE/longitudinal_data_processing.py
        │                                        │
        ▼                                        ▼
 LLM_NEPC_classifier_labels.tsv          longitudinal_prediction_data.csv
                                                 │
                                                 ▼  build_prediction_inputs.py     (+ build_genomic_inputs.py)
                                          prediction_inputs/  (aggregated + longitudinal + split + horizons)
                                                 │
                                                 ▼  cox_aggregated / landmark_xgboost / dynamic_deephit
                                                    cox_pgs_adjusted / cox_genomic_univariate
                                                 │
                                                 ▼  generate_figures.ipynb
                                          figures/
```

The NEPC labels and the survival models are **independent consumers** of the stage-1 outputs;
the LLM labels are not (currently) fed into the survival models.

---

## Stage 1 — Compile prostate cohort source data

`data_preprocessing/compile_prostate_data.py` (module-level script, no CLI). Derives the prostate
MRN set from the DFCI first-treatments table, loads batched note JSONs into `prostate_text_data.csv`,
then filters the raw ICD / health-history / medication / lab / somatic exports down to the cohort and
derives `total_psa_records.csv` and `platinum_chemo_records.csv`.

- **Inputs (hard-coded under `DATA_PATH = /data/gusev/USERS/jpconnor/data/`):**
  `first_treatments_dfci_w_inferred_cancers.csv`, `full_VTE_embeddings_metadata.csv`,
  `VTE_notes_with_full_metadata_batch_*.json`, `timestamped_icd_info.csv.gz`, `HEALTH_HISTORY.csv`,
  `MEDICATIONS.csv`, `OUTPT_LAB_RESULTS_LABS.csv`, `complete_somatic_data_df.csv`.
- **Outputs (under `NEPC_PROJ_PATH = DATA_PATH/CAIA/COMPASS/`):** the eight `prostate_*` /
  `*_records.csv` tables listed in the data-flow diagram.
- **Cohort definition:** "prostate patients **that have batched note text**" — the related tables are
  filtered against the notes dataframe (`complete_df`), not the bare prostate MRN list.

**Note source — `prostate_text_data.csv`.** This CSV is the single note source shared by all LLM
pipelines. It is produced by `data_preprocessing/compile_prostate_notes.py` (CLI), which extracts every
available note for a prostate MRN list directly from the raw OncDRS clinical-text sources and writes the
standardized `DFCI_MRN, EVENT_DATE, NOTE_TYPE, CLINICAL_TEXT, …` columns. The MRN list is supplied via
`--mrn-file` / `--mrns`, or derived from the inferred-cancer cohort table with `--derive-prostate-mrns`.
(The batched-VTE note path inside `compile_prostate_data.py` that previously generated this file is
deprecated.)

```bash
python COMPASS/data_preprocessing/compile_prostate_notes.py --derive-prostate-mrns
# or: --mrn-file prostate_mrns.txt
```

`compile_MRNs_for_manual_review.py` and `compile_text_for_LLM_review.py` build manual-review / LLM-review
sheets from the stage-1 outputs (platinum mentions, non-prostate-primary ICDs, PARPi exposure, BRCA2 status,
±90-day note windows). These are auxiliary; they do not feed the survival pipeline.

---

## Stage 2 — NEPC classifier (LLM phenotyping)

See **`COMPASS/NEPC_classifier/README.md`** — it is the authoritative reference for this stage. Summary:

- One Azure OpenAI (`gpt-4o`) call per patient. Labels each patient `nepc > avpc > biomarker > conventional`
  with independent `has_non_prostate_primary` and `has_molecular_avpc` flags.
- `compile_prostate_note_bundle.py` (optional) pre-compiles raw OncDRS notes into a gzip bundle;
  `run_NEPC_classifier.py` scans for trigger regexes, builds per-patient snippets, calls the LLM, and
  writes `LLM_NEPC_classifier_labels.tsv` (resumable; `--overwrite` to restart).
- All triggers run on `clean_note`-cleaned text from `COMPASS/utils.py`.

**Configuration (env vars, with defaults in `helpers.py`):**

| Env var | Default |
|---|---|
| `CAIA_COMPASS_DATA_PATH` | `/data/gusev/USERS/jpconnor/data/CAIA/COMPASS/` |
| `CAIA_COMPASS_NEPC_CLASSIFIER_OUTPUT_DIR` | `<data path>/LLM_NEPC_labels` |
| `CAIA_AZURE_OPENAI_ENDPOINT` | hard-coded tenant endpoint (see Known issues) |
| `CAIA_AZURE_OPENAI_API_VERSION` | `2024-04-01-preview` |
| `CAIA_AZURE_OPENAI_MODEL` | `gpt-4o` |

Auth is AAD via `DefaultAzureCredential` (no API key in code).

`run_NEPC_classifier.py` now reads `prostate_text_data.csv` by default (precedence: explicit
`--note-bundle-path` if it exists → `--notes-csv` → raw OncDRS JSONs).

---

## Stage 2b — Longitudinal LLM timelines (`longitudinal_LLM/`)

Two **per-note** extraction pipelines that turn the same `prostate_text_data.csv` into patient-level
*timelines*. Unlike the NEPC classifier (one call per patient → one label), these make one LLM call per
trigger-bearing note (copy-forward duplicates collapsed per patient) and then deterministically aggregate
the per-note extractions. Both reuse the NEPC classifier's Azure client / retry / JSON-parsing / note
loaders via `longitudinal_LLM/helpers.py`, run triggers on `clean_note`-cleaned text, are resumable (a
`*_processed_notes.tsv` log tracks finished notes + failures), and accept `--overwrite`. Outputs default
under `CAIA_COMPASS_DATA_PATH`.

**1. Gleason timeline — `extract_gleason_timeline.py`** (`→ LLM_gleason_timeline/`)
Trigger: any mention of Gleason / Grade Group / ISUP. Each call extracts every documented Gleason score
with the date the grade was assigned (specimen date if stated, else the note date — recorded in
`date_source`), specimen type, and whether it is a historical reference. Aggregation validates patterns
(1–5) / totals (2–10), derives the ISUP Grade Group when absent, and de-duplicates per patient.
`gleason_timeline.tsv` = one row per distinct `(score, date, specimen)` — every Gleason score the patient
received, with its date. (`gleason_extractions_raw.tsv` keeps pre-dedup per-note provenance.)

**2. AVPC / NEPC criteria timeline — `extract_avpc_nepc_timeline.py`** (`→ LLM_avpc_nepc_timeline/`)
Triggers: the NEPC classifier's `nepc` + `avpc` regexes. Each call records which criteria are documented
as present — the 7 Aparicio aggressive-variant criteria (`C1`–`C7`) plus four discrete NEPC sub-features
(`NEPC:small_cell_dx`, `NEPC:histologic_transformation`, `NEPC:ne_features`, `NEPC:positive_ne_ihc`) — with
each finding's diagnosis date. Aggregation takes each criterion's **earliest** documented date as its onset.
`avpc_nepc_timeline.tsv` = one row each time a **new** criterion is first added to a patient's record,
carrying the `cumulative_criteria` set documented up to that date.

```bash
python COMPASS/longitudinal_LLM/extract_gleason_timeline.py --max-workers 4
python COMPASS/longitudinal_LLM/extract_avpc_nepc_timeline.py --max-workers 4
# scope/test first with e.g. --mrn-file <mrns.txt> --limit-notes 50
```

**Cost levers (Gleason especially).** Gleason is mentioned in nearly every prostate note, so a
full-cohort run is one LLM call per Gleason-mentioning note. Two flags control this:
- `--note-types Pathology` — Gleason is *assigned* in pathology; restricting to it is far cheaper and
  higher-fidelity (default is all notes, per "any mention triggers collection").
- `--context-chars N` — chars kept on each side of a match (Gleason default **600**, criteria **2000**).
  Smaller windows raise the copy-forward dedup hit-rate and shrink per-call tokens; the criteria
  pipeline keeps a wide window because it must judge e.g. ">= 5 cm" measurements and visceral-vs-bone.

---

## Stage 3 — Survival analysis

Two arms share one library (`helpers/`) and one set of model implementations (`PROFILE/`):

- **PROFILE arm** — the real implementations. Reads `longitudinal_prediction_data.csv`.
- **CAIA arm** — thin wrappers (`runpy.run_module`) that inject CAIA defaults (`--id-col person_id`,
  `--age-col AGE_AT_DIAGNOSIS`, a `.parquet` `--data`, CAIA output dirs) and delegate to the PROFILE
  module. The only real data-layer difference is `helpers.loaders.load_caia_parquet`, which PROFILE's
  `build_prediction_inputs.py` already auto-selects when `--data` ends in `.parquet`. **There is no
  copy of the analysis logic in CAIA** — keep it that way; edit PROFILE, not CAIA.

### 3.1 — `longitudinal_data_processing.py` → `longitudinal_prediction_data.csv`

Consolidates/standardizes labs (via `helpers/consolidate_dfci_labs.py`), attaches the first prostate
(`C61`) diagnosis date and outcomes, rebases all timing to `FIRST_RECORD_DATE = min(first lab, diagnosis,
first treatment)`, and applies patient-level cohort filters.

- **Cohort filters (`apply_cohort_filters`, logged per step):** C61 prostate required & non-prostate
  primary excluded (done at ICD stage); `FIRST_TREATMENT == 1`; ≥ 5 PSA rows; PARPi-exposed patients dropped.
- **Lab QC (`consolidate_dfci_labs`):** unit standardization to canonical units, sentinel nulling
  (e.g. `9999999`), physiologic-range nulling, combined-BP splitting. Out-of-range values are **nulled,
  not row-dropped** — downstream must filter on `conversion_status` (or pass `--successful-only`).
- **Timing semantics:** `t_lab`, `t_diagnosis`, `t_first_treatment`, `t_platinum`, `t_last_contact`,
  `t_death`. ⚠️ `t_death` is set equal to `t_last_contact` for everyone (no true death date in the source) —
  see Known issues.

### 3.2 — `build_prediction_inputs.py` → `prediction_inputs/`

The **single source of truth for model inputs**. For each landmark it cohort-filters, intersects MRNs
across landmarks, derives the train/valid/test split **once on the base landmark**, and writes per-landmark
aggregated + longitudinal tables plus shared split / canonical-lab / horizon artifacts.

- **Key CLI:** `--data`, `--landmark-days 0 90` (default from `cox_aggregated.DEFAULT_LANDMARK_DAYS`),
  `--seed`, `--test-frac`, `--val-frac`, `--time-unit-days 7`, `--min-patient-coverage`, `--max-labs`,
  `--auc-quantiles`, `--id-col`, `--age-col`, `--no-canonical-labs`.
- **Outputs:** `aggregated_landmark{D}.csv`, `longitudinal_landmark{D}.csv` (+ `_manifest.json`),
  `pre_treatment_lab_long_landmark{D}.csv`, `split_assignments.csv`, `landmark_mrn_availability.csv`,
  `canonical_labs_train_val.csv`, `build_manifest.json`.
- `build_genomic_inputs.py` builds the parallel `prediction_inputs/genomic/` arm (index time =
  `SAMPLE_COLLECTION_DT`, plus 12 binary genomic indicators `{TP53,RB1,PTEN}×{SV,DEL,AMP,SNV}`). It
  **reuses** the main `split_assignments.csv` so test stays test.

### 3.3 — Models

All read prebuilt inputs and the `split` column; none re-derive the split. Endpoints: `platinum`
(time to first platinum), `death`. Landmarks default `[0, 90]`. Metrics: Harrell C-index, IPCW mean
AUC(t), integrated IPCW Brier — horizons come from `build_manifest.json` so all models share a grid.

| Script | Model | CLI notes |
|---|---|---|
| `cox_aggregated.py` | Cox: univariate (n_obs-adjusted) + elastic-net multivariable (sksurv `CoxnetSurvivalAnalysis`, 5-fold CV, AGE unpenalized) | `--analysis {univariate,multivariable,both}`, `--landmark-days`, `--endpoints`, `--n-folds` |
| `landmark_xgboost.py` | XGBoost `survival:cox`, 5-fold CV grid (`max_depth × eta × min_child_weight`) | `--landmark-days`, `--endpoints`, `--max-features` |
| `dynamic_deephit.py` | GRU discrete-time competing-risks DeepHit | `--landmark-day` (**singular**), `--config {competing,PLATINUM,DEATH}` |
| `cox_pgs_adjusted.py` | PGS-adjusted univariate Cox sensitivity (Testosterone, PSA) | `--landmarks`, `--target-labs`, `--feature-stat` |
| `cox_genomic_univariate.py` | Univariate Cox on the genomic (`t_sample`) arm | `--inputs-dir <…>/genomic` |

`cox_aggregated.py` also **is the shared library** — `make_outcome_df`, `build_feature_matrix`,
`build_model_matrices`, `compute_ipcw_auc_t`, `_make_survival_array`, `_load_prebuilt_landmark`,
`_load_build_manifest` are imported by the other scripts. (Note: `helper.py` defines its own copies of
a few of these — `combined_event_label`, `choose_stratification_labels`, `_make_survival_array` — and
both copies are live; consolidate carefully.)

### 3.4 — Notebooks

- `PROFILE/run_locally.ipynb` — drives stages 1→3 in one kernel via `subprocess`/`!{PYTHON}`. Section 5
  loops a `TASKS` list (cox + xgboost at landmarks 0/90; deephit commented out) then a PGS sweep.
  ⚠️ contains a comment-inside-list bug, see Known issues.
- `PROFILE/generate_figures.ipynb` — 4 platinum-arm figures (paired univariate volcano, grouped AUC
  barplot, 2×2 importance grid, PGS interpretation). Writes `.png`+`.pdf` to `figures/`.
- `CAIA/summary_figures.py` (+ `.ipynb`) — standalone PROFILE-vs-CAIA cohort comparison
  (time distributions, platinum incidence, overall-survival KM, per-lab raw + IRNT histograms).

---

## Conventions & invariants (preserve these when editing)

1. **The split is derived once, in `build_prediction_inputs.py`, on the base landmark.** The base landmark
   is `--landmark-days[0]` — **order matters**. Every model reads `split_assignments.csv`; never re-split
   inside a model script. `build_genomic_inputs.py` reuses the same file.
2. **Fit on train only; never touch test for fitting.** Imputers, `StandardScaler`, canonical-lab
   selection, longitudinal clip bounds, and Breslow baselines are all fit on train (or train-fold) and
   applied to eval. Per-fold canonical labs are recomputed inside CV. The leakage guards
   `assert_no_test_leakage` / `assert_disjoint_folds` live in `helper.py` (note: the build scripts do not
   currently call them — see Known issues).
3. **Boundary asymmetry to be aware of:** the aggregated/Cox arm selects canonical labs & AUC horizons on
   **train+valid**; the longitudinal arm fits clip bounds & lab selection on **train only**. Intentional,
   but keep it consistent if you change one.
4. **Endpoints and durations:** `platinum` uses `(t_platinum, PLATINUM)`; `death` uses `(t_death, DEATH)`.
   For non-events, the anchor time is filled with `t_last_contact` (censoring). After landmark rebasing,
   the validity filter requires duration `> 0`, which silently drops patients with an event before/at the
   landmark — add count logging if you depend on it.
5. **ID/age columns are injected at runtime.** PROFILE defaults to `DFCI_MRN` / `AGE_AT_TREATMENTSTART`;
   CAIA injects `person_id` / `AGE_AT_DIAGNOSIS`. `build_*` and model `main()` functions mutate module
   globals **and monkey-patch `cox_aggregated.ID_COL/AGE_COL`**. If you add a function that captures
   `ID_COL` at import time (default arg, module constant), it will not see the patch — thread the column
   through as a parameter instead.
6. **Horizon grid is shared via `build_manifest.json`** so Cox/XGBoost/DeepHit AUC & Brier are comparable.
   Don't compute horizons ad hoc in a model script.
7. **Edit PROFILE, not CAIA.** CAIA files are `runpy` shims; the only CAIA-specific real code is
   `summary_figures.py` and the parquet loader.

---

## Configuration & paths

- **Hard-coded cluster roots** (all overridable by CLI except module-level constants):
  - Data: `/data/gusev/USERS/jpconnor/data/CAIA/COMPASS/`
  - PROFILE results: `/data/gusev/USERS/jpconnor/data/CAIA/COMPASS/survival_analysis/PROFILE`
  - CAIA results: `…/survival_analysis/CAIA`
  - Figures: `/data/gusev/USERS/jpconnor/figures/CAIA/COMPASS/`
- `helpers/consolidate_dfci_labs.py` expects `OMOP_to_DFCI_lab_ids.csv` and `unique_lab_ids_w_units.csv`
  directly under `survival_analysis/` (`PROFILE_DIR = parents[1]`), **not** under `PROFILE/`.

## Recommended run order

```bash
# Stage 1 (cluster paths hard-coded)
python COMPASS/data_preprocessing/compile_prostate_data.py
python COMPASS/data_preprocessing/compile_prostate_notes.py --derive-prostate-mrns   # → prostate_text_data.csv

# Stage 2 — see COMPASS/NEPC_classifier/README.md
python COMPASS/NEPC_classifier/run_NEPC_classifier.py --mrn-file <mrns.txt> --max-workers 4

# Stage 2b — longitudinal LLM timelines (per-note)
python COMPASS/longitudinal_LLM/extract_gleason_timeline.py --max-workers 4
python COMPASS/longitudinal_LLM/extract_avpc_nepc_timeline.py --max-workers 4

# Stage 3 — or just run PROFILE/run_locally.ipynb top to bottom
python COMPASS/survival_analysis/PROFILE/longitudinal_data_processing.py
python COMPASS/survival_analysis/PROFILE/build_prediction_inputs.py --landmark-days 0 90 --time-unit-days 7
python COMPASS/survival_analysis/PROFILE/build_genomic_inputs.py
python COMPASS/survival_analysis/PROFILE/cox_aggregated.py    --inputs-dir <…>/prediction_inputs --landmark-days 0
python COMPASS/survival_analysis/PROFILE/landmark_xgboost.py  --inputs-dir <…>/prediction_inputs --landmark-days 0
python COMPASS/survival_analysis/PROFILE/dynamic_deephit.py   --inputs-dir <…>/prediction_inputs --landmark-day 0 --config competing
```

## Dependencies

No packaged environment is checked in. Assumed: `pandas`, `numpy`, `scipy`, `tqdm`, `scikit-learn`,
`scikit-survival` (`sksurv`), `xgboost`, `lifelines`, `torch` (DeepHit), `matplotlib`, `pyarrow`
(CAIA parquet), and `openai` + `azure-identity` (LLM labeling). Python **3.12+** is recommended (one
preprocessing script uses 3.12-only f-string nesting — see Known issues).

---

## Known issues / footguns

These are real, verified items found in code review. Fix opportunistically; at minimum, don't be
surprised by them.

### High impact

- **`run_locally.ipynb` swallows a comma in a comment.** `"--endpoints", "platinum"  # death excluded for now,`
  — the comma is inside the comment, so Python concatenates `"platinum" "--n-folds"` →
  `"platinum--n-folds"` (3 occurrences: cox, xgboost, PGS). Move the comment out of the list literal.
- **`t_death == t_last_contact` for all patients** (`longitudinal_data_processing.py:208`, and the same in
  `loaders.py` for CAIA). The death **event indicator** is real but the **duration** equals censoring time,
  so dead and censored patients have identical durations on the death endpoint. Confirm the source truly
  lacks death dates before trusting any death-endpoint model/Brier.
- **`compile_MRNs_for_manual_review.py` won't run on Python < 3.12** — nested same-quote f-strings
  (`f'…{df['col']}…'`, lines ~120-127) are a `SyntaxError` before 3.12.
- **Non-prostate-primary ICD leak** (`compile_MRNs_for_manual_review.py:67`): exclusion is
  `codes != 'C61'`, so a decimal-form prostate code like `C61.9` (which parses `number=61`, inside the
  C00–C75 range) is mislabeled a non-prostate primary. Exclude on `number == 61` instead.
- **Platinum-meds fan-out** (`longitudinal_data_processing.py`): the platinum table is left-joined without
  collapsing to one row per MRN, so a patient with two platinum meds duplicates every lab row. Collapse to
  earliest platinum date before the merge.

### Medium impact

- **Over-broad note-cleaning regex** (`utils.py:72`, `vitals_block`): the unit alternation includes the
  single letters `m`/`C`/`F`, so a `BP`/`Temp`/… match runs to the first `c`/`f`/`m` on the line and can
  delete legitimate clinical text. Require word-boundaried multi-char units.
- **`make_row` corrupts list fields if the LLM returns a bare string** (`run_NEPC_classifier.py:127+`):
  `" | ".join(str(g) for g in (result.get("biomarker_genes") or []))` iterates the **characters** of a
  string (`"BRCA2"` → `B | R | C | A | 2`). Coerce to list first.
- **`_inject_defaults` in the CAIA wrappers only matches bare `--flag`, not `--flag=value`** — passing
  `--data=foo.parquet` appends a duplicate default `--data`.
- **Hard-coded `DFCI_MRN` in a PROFILE error message** (`cox_aggregated.py` "Input data must contain
  DFCI_MRN") is misleading on CAIA `person_id` runs — use `ID_COL`.
- **`auc_max_time_units = 260` is hard-coded** in `cox_aggregated.py` and `landmark_xgboost.py` and still
  admin-censors AUC/Brier, despite nearby comments claiming admin censoring was removed.
- **Leakage guards are never invoked** by `build_prediction_inputs.py` / `build_genomic_inputs.py` — the
  split is constructed correctly but the cheap `assert_no_test_leakage` calls aren't wired in.
- **Silent patient drops** at several inner-joins and `valid`-mask filters (genomic `t_sample` filter,
  diagnosis/death inner joins, duration `> 0` filter). `apply_cohort_filters` is the good model — it logs
  every step; the build scripts should too.
- **`cox_pgs_adjusted.py` fits on the full cohort including test** (no `split` filter), unlike
  `cox_genomic_univariate.py`. Acceptable for a univariate sensitivity arm, but undocumented.
- **`summary_figures.py:108/111`** `.get("PLATINUM", 0).astype(bool)` turns NaN into `True`
  (mis-labels censored as event); mirror the `pd.to_numeric().fillna(0).astype(int)` pattern used elsewhere.

### Low impact / cleanliness

- Two divergent copies of helper functions (`cox_aggregated.py` vs `helper.py`) risk drift.
- `iterrows`/`apply`-based row loops in `consolidate_dfci_labs.py` and `longitudinal_data_processing.py`
  are slow on full DFCI-scale pulls — vectorize if performance bites.
- Dead code: `run_univariate_associations` / `build_aligned_cohort` (cox_aggregated),
  `fixed_horizon_grid_from_targets` (deephit), `--verbose-eval` (xgboost), `mrns_with_note_counts` and the
  unused `total_psa` read (data_preprocessing).
- Hard-coded Azure endpoint default committed in `NEPC_classifier/helpers.py` (infra exposure, not a
  secret — auth is AAD).
- The CAIA `generate_figures.ipynb` is a copy-paste of PROFILE's rather than a shared module.

---

## Notes

- `.ipynb_checkpoints/` and `__pycache__/` artifacts exist in the tree but are git-ignored and not part of
  the workflow.
- Several scripts read and write data **outside** the repository root (the `/data/gusev/...` cluster paths).
- `NEPC_classifier` is the only supported labeling pipeline; earlier LLM-labeling code has been removed.
